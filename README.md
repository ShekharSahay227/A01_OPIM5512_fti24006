# A01_OPIM5512_fti24006
A01 - First assignment for OPIM 5512 - Data Science using Python


## How to Run

1. Install the required dependencies using below command:
py -3.11 -m pip install -r requirements.txt

2. Run the Python script:
py -3.11 src/boxplot.py


## Expected Output

1. The python script will load the California Housing dataset
2. It will display first few rows on dataset
3. It will print the dataset shape
4. It will generate a boxplot visualization
5. Output boxplot will be saved as image - boxplot.png in the 'figs' folder